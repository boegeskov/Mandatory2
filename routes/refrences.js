const router = require("express").Router(); // den sidste parantes instansere linjen. // dummy router


router.get("/api/refrences", (req, res) => {
    
});


// a variable you assaign something too
module.exports = {
    router
};